# Create new python app

-Create new python app, for example print("Hello World from Python in Docker!")
-Run it without Docker
-Wrap it with docker image
-Docker run
-Make sure your get output based on print
